services:
  - type: web
    name: bridge-four
    runtime: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
